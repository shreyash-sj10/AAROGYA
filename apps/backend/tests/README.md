# Backend tests

**CI vs local:** See [docs/TESTS_INVENTORY.md](../../docs/TESTS_INVENTORY.md). **`npm run verify`** (repo root) includes **`test:phase1`** (determinism + P0 + startup/contracts).

**Match CI locally (from repo root):** `npm run verify`
