# P0 safety suite (Phase 1.2)

Canonical **CI** entry: `npm run test:p0-ci` (`scripts/run-p0-ci.js`).

Covers **allergy / hard-constraint** rejection, **catalog P0 rules**, **fallback** paths without unsafe meals, and **AI profile** forbidden-field rejection.
