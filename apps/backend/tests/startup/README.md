# Startup & schema integrity (Phase 1.3–1.4)

Canonical **CI** entry: `npm run test:startup-contracts-ci` (`scripts/run-startup-contracts-ci.js`).

Combines **canonical rulebase / rule-schema** checks with **contract schema smoke** (AJV compile + minimal valid payload).
