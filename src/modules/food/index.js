const {
  foodSchema,
  getFoodsByCategory,
  loadAllFoods,
  loadSampleFoods,
  validateFood,
} = require("./food.service");

module.exports = {
  foodSchema,
  getFoodsByCategory,
  loadAllFoods,
  loadSampleFoods,
  validateFood,
};
